import React from "react";
import {Typography} from "@mui/material";

export default function HomeView() {

      return (
          <div>
            <Typography variant={"h1"}>HomeView</Typography>
          </div>
      );
}
